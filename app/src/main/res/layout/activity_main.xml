<!-- În grid, adaugă un rând nou după Diagnostic -->
<Button
    android:id="@+id/btn_start_hce"
    style="@style/MenuTile"
    android:text="Start HCE"
    android:backgroundTint="#4CAF50"
    app:layout_column="0"
    app:layout_row="1"
    app:layout_columnSpan="2" />

<!-- Mută celelalte butoane mai jos (crește rowCount la 6) -->